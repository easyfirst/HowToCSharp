namespace _02Exceptions
{
    /// <summary>
    /// The list starting with 0 .
    /// The value can be defined separately. Example: Two = 2
    /// Otherwise, as a single line item, each element increases by one.
    /// </summary>
    public enum Situations
    {
        One, Two,
        Null
    }
}